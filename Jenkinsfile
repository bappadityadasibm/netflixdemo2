node {
        stage 'Build'
        echo 'Starting Build Stage'
        //dir('blog-microservices') {
        // some block
        //}
        sh './build-all.sh'
        
        stage('Test') {
        echo 'Test Stage'
        }
}
